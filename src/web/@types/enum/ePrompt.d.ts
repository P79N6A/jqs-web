/**
 * Created by garrettzhou@2018-9-1
 * prompt 常用枚举
 */
declare namespace ePrompt {
    const enum Type {
        Modal, Message
    }
    const enum StyleType {
        Warning, Info, Error, Success, Confirm
    }
}
